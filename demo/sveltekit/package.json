{
  "name": "@glyphnav-demo/sveltekit",
  "version": "0.0.0",
  "private": true,
  "description": "SvelteKit demo for glyphnav, part of the combined demo deploy.",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@glyphnav-demo/shared": "workspace:*",
    "glyphnav": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "3.0.10",
    "@sveltejs/kit": "2.69.3",
    "@sveltejs/vite-plugin-svelte": "7.2.0",
    "svelte": "5.56.5",
    "svelte-check": "4.7.3",
    "typescript": "6.0.3",
    "vite": "8.1.5"
  },
  "//typescript": "svelte-check (the only tsc consumer here) needs the JS Compiler API that the Go-native typescript@7 no longer ships; stays 6.x until svelte-check supports TS7. Checked 2026-07: svelte-check 4.7 added experimental --tsgo/--tsgo-experimental-api (verified working here) but they resolve only the @typescript/native-preview nightly, not the typescript@7 GA package, and kit's typescript peer still caps at ^6"
}
