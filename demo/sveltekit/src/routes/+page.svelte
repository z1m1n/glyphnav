<script lang="ts">
  import DemoView from '$lib/DemoView.svelte';
</script>

<DemoView
  title="Home"
  body="SvelteKit edition — filesystem routing on Svelte's official framework. attachGlyphnav(goto) installs a capture-phase <a> click listener, so every link decodes the URL and hands the navigation to goto. With commit: 'navigate first' the route swaps instantly and the bar animates on top."
/>
