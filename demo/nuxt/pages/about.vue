<template>
  <DemoView
    title="about"
    body="Because the plugin wraps the router, navigateTo() animates too — no per-link wiring. Deep links with ?query and #hash animate just the same; they are part of the path. Hash-mode routers work as well via historyMode: 'hash'."
  />
</template>
