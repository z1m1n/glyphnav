<template>
  <DemoView
    title="Features"
    body="The animation rides on <code>history.replaceState</code>; Vue Router (under Nuxt) performs the real navigation. Modified clicks (⌘/Ctrl/middle) fall through to the browser, exactly like a normal link. Pass <code>intercept: 'none'</code> to leave the router untouched and animate only explicit calls."
  />
</template>
