{
  "name": "@glyphnav-demo/nuxt",
  "version": "0.0.0",
  "private": true,
  "description": "Nuxt demo for glyphnav, part of the combined demo deploy.",
  "type": "module",
  "scripts": {
    "dev": "nuxi dev --no-open",
    "build": "nuxi build",
    "generate": "nuxi generate",
    "typecheck": "nuxi prepare && vue-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@glyphnav-demo/shared": "workspace:*",
    "glyphnav": "workspace:*",
    "nuxt": "4.4.8"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vue-tsc": "3.3.7"
  },
  "//typescript": "vue-tsc (the only tsc consumer here) needs the JS Compiler API that the Go-native typescript@7 no longer ships; stays 6.x until vue-tsc supports TS7"
}
