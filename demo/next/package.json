{
  "name": "@glyphnav-demo/next",
  "version": "0.0.0",
  "private": true,
  "description": "Next.js (App Router) demo for glyphnav, part of the combined demo deploy.",
  "type": "module",
  "scripts": {
    "dev": "node dev.mjs",
    "build": "NEXT_TELEMETRY_DISABLED=1 next build",
    "start": "NEXT_TELEMETRY_DISABLED=1 next start",
    "//typecheck": "Needs next-env.d.ts (generated by dev/build); run after a build, or use `next build` which also typechecks.",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@glyphnav-demo/shared": "workspace:*",
    "glyphnav": "workspace:*",
    "next": "16.2.11",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "typescript": "6.0.3"
  },
  "//typescript": "next build's type-check step needs the JS Compiler API that the Go-native typescript@7 no longer ships (Next then thinks TypeScript is missing); stays 6.x until Next supports TS7. Checked 2026-07-22 on next 16.2.11: still blocked — `next build` type-check phase reports TypeScript 'not installed' with typescript@7 and crashes (auto-install then `id must be of type string`). 16.2.11 only added @typescript/native-preview detection (warns build type-checking needs the standard typescript package); no useTypeScriptCli release yet (vercel/next.js#95639)"
}
