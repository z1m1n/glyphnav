import { View } from '../View';

export default function Blog() {
  return (
    <View
      title="Blog"
      body="The animation rides on history.replaceState; the Next router performs the real navigation. Modified clicks (⌘/Ctrl/middle) fall through to the browser, exactly like a normal link. The same adapter also drives the Pages Router via routerMode: 'pages'."
    />
  );
}
