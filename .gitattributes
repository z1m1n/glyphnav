# Normalize line endings to LF in the working tree for every text file, so the
# CI format check (oxfmt, LF-only) is deterministic no matter the contributor's
# OS or git `core.autocrlf` setting. Binary files are auto-detected by
# `text=auto` and left untouched.
* text=auto eol=lf
